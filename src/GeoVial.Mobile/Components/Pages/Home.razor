@page "/"
@inject GeoVial.Mobile.Servicios.EstadoSesion Sesion
@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
        => Nav.NavigateTo(Sesion.EstaAutenticado ? "/relevamientos" : "/login", replace: true);
}
